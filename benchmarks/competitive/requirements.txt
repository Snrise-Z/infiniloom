# Competitive Benchmark Requirements

# Core dependencies
psutil>=5.9.0          # Process and memory monitoring
GitPython>=3.1.30      # Git repository cloning
pyyaml>=6.0           # YAML parsing
toml>=0.10.2          # TOML parsing

# Note: hyperfine is a CLI tool, not a pip package. Install via: brew install hyperfine

# LLM APIs
anthropic>=0.18.0      # Claude API
openai>=1.12.0         # GPT-4 API

# Reporting
tabulate>=0.9.0        # Table formatting
matplotlib>=3.7.0      # Charts
pandas>=2.0.0          # Data analysis

# Token counting (for comparison)
tiktoken>=0.5.0        # OpenAI tokenizer

# Development
pytest>=7.0.0          # Testing
black>=23.0.0          # Formatting
